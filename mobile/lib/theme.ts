/**
 * Palette de référence des prototypes HTML (encre / lavande / corail).
 * Polices (Fraunces + Manrope) pas encore intégrées dans ce scaffold —
 * à ajouter via @expo-google-fonts avant la passe de direction
 * artistique finale.
 */
export const colors = {
  background: "#14121f",
  surface: "#1e1b2e",
  surfaceAlt: "#28243a",
  border: "#332f47",
  text: "#f5f3ff",
  textMuted: "#a9a3c2",
  primary: "#b8a9ff",
  accent: "#ff8a73",
  danger: "#ff5470",
};

export const spacing = (n: number) => n * 8;
